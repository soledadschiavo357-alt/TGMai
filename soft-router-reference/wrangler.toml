name = "soft-router-reference"
main = "src/worker.js"
compatibility_date = "2023-10-25"

# 绑定自定义域名 (可选)
# routes = [
#   { pattern = "shop.example.com", custom_domain = true }
# ]

[vars]
# 可以在这里配置环境变量
API_HOST = "api.example.com"
